{
  "name": "trello-mcp-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "node scripts/check-lockfile.mjs && vitest run",
    "test:watch": "vitest",
    "lockfile:fix": "node scripts/fix-lockfile.mjs",
    "lockfile:check": "node scripts/check-lockfile.mjs"
  },
  "dependencies": {
    "@cloudflare/workers-oauth-provider": "^0.4.0",
    "@modelcontextprotocol/sdk": "1.29.0",
    "agents": "0.11.9",
    "zod": "4.4.3"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "4.20260515.1",
    "typescript": "5.7.3",
    "vitest": "^1.6.1",
    "wrangler": "4.92.0"
  }
}
